[project]
name = "rogue-dns-scanner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dnspython>=2.8.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]

[project.scripts]
rogue-dns-scanner = "rogue_dns_scanner.cli:main"
